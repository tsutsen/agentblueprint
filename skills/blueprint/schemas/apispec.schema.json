{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apispec.schema.json",
  "title": "ApiSpec",
  "description": "Interface specification for all functions exposed by a module. Source of truth for test spec and cross-spec linting. Version 2.0.0.",
  "type": "object",
  "required": [
    "version",
    "module",
    "functions",
    "project"
  ],
  "additionalProperties": false,
  "definitions": {
    "typeRef": {
      "type": "string",
      "description": "A type name: primitive, entity name, enum name, or array thereof (e.g. 'string', 'User', 'OrderStatus', 'OrderItem[]'). Use 'void' for functions that return nothing."
    },
    "parameterObject": {
      "type": "object",
      "required": [
        "name",
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "camelCase parameter name.",
          "pattern": "^[a-z][A-Za-z0-9]*$"
        },
        "type": {
          "$ref": "#/definitions/typeRef",
          "description": "The type of this parameter. Must resolve to a primitive, entity, or enum in DataSpec."
        },
        "required": {
          "type": "boolean",
          "default": true,
          "description": "Whether this parameter is required. Default: true."
        },
        "description": {
          "type": "string",
          "description": "Description of this input parameter."
        },
        "example": {
          "description": "A concrete example value for this parameter."
        }
      }
    },
    "errorCondition": {
      "type": "object",
      "required": [
        "code",
        "condition"
      ],
      "additionalProperties": false,
      "properties": {
        "code": {
          "type": "string",
          "description": "Machine-readable error code, e.g. 'NOT_FOUND', 'UNAUTHORIZED'.",
          "pattern": "^[A-Z][A-Z0-9_]*$"
        },
        "condition": {
          "type": "string",
          "description": "Human-readable description of when this error is thrown."
        },
        "returnType": {
          "type": "string",
          "description": "What the caller receives: exception class name, HTTP status, or error shape."
        }
      }
    },
    "fnId": {
      "type": "string",
      "description": "Function ID from ApiSpec. Format: FN-camelCase, e.g. 'FN-createUser'.",
      "x_idPattern": "fn"
    },
    "glId": {
      "type": "string",
      "description": "Glossary term identifier, format GL-NNN-PascalCase.",
      "x_idPattern": "gl"
    },
    "scrId": {
      "type": "string",
      "description": "Screen identifier from DesignSpec, format SCR-NNN-PascalCase.",
      "x_idPattern": "scr"
    },
    "ujId": {
      "type": "string",
      "description": "User journey identifier from DesignSpec, format UJ-NNN-lowerCamelCase.",
      "x_idPattern": "uj"
    },
    "compId": {
      "type": "string",
      "description": "Component identifier from ArchitectureSpec, format COMP-NNN-PascalCase.",
      "x_idPattern": "comp"
    },
    "reqId": {
      "type": "string",
      "description": "^REQ identifier.",
      "x_idPattern": "req"
    },
    "nfrId": {
      "type": "string",
      "description": "^NFR identifier.",
      "x_idPattern": "nfr"
    },
    "conId": {
      "type": "string",
      "description": "Constraint identifier from ArchitectureSpec, format CON-NNN-Name.",
      "x_idPattern": "con"
    }
  },
  "properties": {
    "version": {
      "type": "string",
      "description": "Semver version of this api document instance.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "schemaVersion": {
      "type": "string",
      "description": "Version of the ApiSpec schema this document conforms to. Must be '2.0.0'.",
      "const": "2.0.0"
    },
    "module": {
      "type": "string",
      "description": "Must match the module name in the corresponding data spec.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "High-level description of this API spec module. Summarizes the module's purpose and scope."
    },
    "dataSpecVersion": {
      "type": "string",
      "description": "Version of the data spec this API spec was authored against. Used for drift detection.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "functions": {
      "type": "array",
      "description": "All functions exposed by this module.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "name",
          "inputs",
          "output"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "$ref": "#/definitions/fnId",
            "description": "Stable unique identifier for this function, e.g. 'FN-placeOrder'. Used by data spec apiRef and test spec fnRef.",
            "x_idPattern": "fn"
          },
          "name": {
            "type": "string",
            "description": "camelCase function name as it appears in code.",
            "pattern": "^[a-z][A-Za-z0-9]*$"
          },
          "description": {
            "type": "string",
            "description": "One sentence: what this function does."
          },
          "entity": {
            "type": "string",
            "description": "Entity this function primarily operates on. Must match an entity name in the data spec.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "inputs": {
            "type": "array",
            "description": "List of input parameters.",
            "items": {
              "$ref": "#/definitions/parameterObject"
            }
          },
          "output": {
            "type": "object",
            "required": [
              "type"
            ],
            "additionalProperties": false,
            "description": "Return value of the function.",
            "properties": {
              "type": {
                "$ref": "#/definitions/typeRef",
                "description": "The return type of this function. Must resolve to a primitive, entity, or enum in DataSpec, or void."
              },
              "description": {
                "type": "string",
                "description": "Description of the return value."
              },
              "example": {
                "description": "A concrete example of the return value."
              },
              "name": {
                "type": "string",
                "description": "Name of the output variable/field."
              },
              "Type": {
                "type": "string",
                "description": "Must resolve to a type in DataSpec, or void."
              }
            }
          },
          "errors": {
            "type": "array",
            "description": "All documented error conditions.",
            "items": {
              "$ref": "#/definitions/errorCondition"
            }
          },
          "visibility": {
            "type": "string",
            "enum": [
              "public",
              "internal"
            ],
            "default": "public",
            "description": "Whether this function is exposed externally or internal only."
          },
          "pure": {
            "type": "boolean",
            "description": "True if this function has no side effects (same input always yields same output). Informs test strategy.",
            "default": false
          },
          "tags": {
            "type": "array",
            "description": "Optional grouping tags, e.g. ['auth', 'admin'].",
            "items": {
              "type": "string"
            }
          },
          "status": {
            "type": "string",
            "description": "Lifecycle status of this function.",
            "enum": [
              "draft",
              "review",
              "confirmed",
              "implemented",
              "deprecated"
            ],
            "default": "draft"
          },
          "priority": {
            "type": "string",
            "description": "Implementation priority. P0 = must have, P1 = should have, P2 = could have, P3 = won't have now.",
            "enum": [
              "P0",
              "P1",
              "P2",
              "P3"
            ],
            "default": "P2"
          },
          "reqRefs": {
            "type": "array",
            "description": "REQ-IDs from GoalSpec that this function implements. Populated during architecture phase. Use for FN\u2192REQ traceability. At least one must be present once requirements are known.",
            "items": {
              "type": "string",
              "$ref": "#/definitions/reqId"
            }
          },
          "nfrRefs": {
            "type": "array",
            "description": "NFR-IDs from GoalSpec that this function helps satisfy (e.g. performance, reliability).",
            "items": {
              "type": "string",
              "$ref": "#/definitions/nfrId"
            }
          },
          "screenRefs": {
            "type": "array",
            "description": "SCR-IDs from DesignSpec screenInventory that this API function supports.",
            "items": {
              "$ref": "#/definitions/scrId"
            }
          },
          "ujRefs": {
            "type": "array",
            "description": "UJ-IDs from DesignSpec userJourneys that this API function enables.",
            "items": {
              "$ref": "#/definitions/ujId"
            }
          },
          "componentRefs": {
            "type": "array",
            "description": "COMP-IDs from ArchitectureSpec components that this function interacts with.",
            "items": {
              "$ref": "#/definitions/compId"
            }
          },
          "constraintRefs": {
            "type": "array",
            "description": "CON-NNN IDs from ArchitectureSpec constraints that this function must respect.",
            "items": {
              "$ref": "#/definitions/conId"
            }
          }
        }
      }
    },
    "glossaryRefs": {
      "type": "array",
      "description": "Glossary term IDs (GL-NNN) referenced in this API spec.",
      "items": {
        "$ref": "#/definitions/glId"
      },
      "uniqueItems": true
    },
    "apiSpecVersion": {
      "type": "string",
      "description": "Version of this API spec (independent of the API version).",
      "default": "1.0.0"
    },
    "functionCount": {
      "type": "integer",
      "description": "Total number of functions defined.",
      "readOnly": true
    },
    "project": {
      "type": "string",
      "description": "Project name. Must match across all specs.",
      "minLength": 1
    },
    "goalSpecVersion": {
      "type": "string",
      "description": "Version of the GoalSpec this spec was reviewed against.",
      "pattern": "^v\\d+\\.\\d+\\.\\d+$"
    }
  },
  "outputFiles": [
    "artifacts/ApiSpec.md",
    "artifacts/ApiSpec.json"
  ],
  "specDependencies": {
    "goalSpec": {
      "required": true
    },
    "architectureSpec": {
      "required": true
    },
    "dataSpec": {
      "required": true
    }
  }
}
