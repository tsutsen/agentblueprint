{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "glossary.schema.json",
  "title": "Glossary",
  "description": "Project vocabulary. Defines all domain terms used across specs. Version 1.0.0.",
  "type": "object",
  "required": [
    "version",
    "schemaVersion",
    "project",
    "terms"
  ],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "schemaVersion": {
      "type": "string",
      "const": "1.0.0"
    },
    "project": {
      "type": "string",
      "description": "Must match project name across all other specs.",
      "minLength": 1
    },
    "terms": {
      "type": "array",
      "description": "All domain terms. Each must be independently understandable.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "term",
          "definition"
        ],
        "additionalProperties": false,
        "properties": {
          "term": {
            "type": "string",
            "description": "The term exactly as it appears in other specs. Case-sensitive.",
            "minLength": 1
          },
          "definition": {
            "type": "string",
            "description": "Precise, unambiguous definition. Must not use the term itself in the definition.",
            "minLength": 10
          },
          "examples": {
            "type": "array",
            "description": "Optional concrete examples that clarify the definition.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "synonyms": {
            "type": "array",
            "description": "Other names for this term used in the project. Synonyms should not have their own entry.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "relatedTerms": {
            "type": "array",
            "description": "GL-NNN identifiers of other terms in this glossary that are closely related.",
            "items": {
              "type": "string",
              "pattern": "^GL-\\d{3}$"
            }
          },
          "category": {
            "type": "string",
            "description": "Optional grouping tag, e.g. 'domain', 'technical', 'process'."
          },
          "id": {
            "type": "string",
            "description": "Unique glossary term identifier. Format: GL-NNN (3-digit zero-padded, sequential).",
            "pattern": "^GL-\\d{3}$"
          }
        }
      },
      "uniqueItems": true
    }
  }
}
