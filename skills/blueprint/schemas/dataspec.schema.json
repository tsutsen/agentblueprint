{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "dataspec.schema.json",
  "title": "DataSpec",
  "description": "Structural specification of all entities, types, and relationships in a system. Source of truth for UML generation and cross-spec linting.",
  "type": "object",
  "required": [
    "version",
    "module",
    "primitives",
    "enums",
    "entities",
    "relationships",
    "project"
  ],
  "additionalProperties": false,
  "definitions": {
    "glId": {
      "type": "string",
      "description": "Glossary term identifier, format GL-NNN-PascalCase.",
      "x_idPattern": "gl"
    },
    "enumId": {
      "type": "string",
      "description": "Enum identifier (NUM-NNN-enumName).",
      "x_idPattern": "num"
    },
    "entId": {
      "type": "string",
      "description": "Entity identifier (ENT-NNN-entityName).",
      "x_idPattern": "ent"
    },
    "relId": {
      "type": "string",
      "description": "Relationship identifier (REL-NNN-relationshipName).",
      "x_idPattern": "rel"
    },
    "primId": {
      "type": "string",
      "description": "Primitive type identifier (PRIM-NNN-primitiveName).",
      "x_idPattern": "prim"
    },
    "compId": {
      "type": "string",
      "description": "Component identifier from ArchitectureSpec, format COMP-NNN-PascalCase.",
      "x_idPattern": "comp"
    },
    "reqId": {
      "type": "string",
      "description": "^REQ identifier.",
      "x_idPattern": "req"
    },
    "nfrId": {
      "type": "string",
      "description": "^NFR identifier.",
      "x_idPattern": "nfr"
    },
    "conId": {
      "type": "string",
      "description": "^CON identifier.",
      "x_idPattern": "con"
    },
    "fnId": {
      "type": "string",
      "description": "Function identifier from ApiSpec, format FN-NNN-slug.",
      "x_idPattern": "fn"
    }
  },
  "properties": {
    "version": {
      "type": "string",
      "description": "Semver string, e.g. '1.0.0'. Increment when any entity, field, or relationship changes.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "schemaVersion": {
      "type": "string",
      "description": "Version of the DataSpec schema this document conforms to. Must be '1.0.0'.",
      "const": "1.0.0"
    },
    "module": {
      "type": "string",
      "description": "Human-readable name of the system or module this spec describes.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "Description of this primitive type."
    },
    "primitives": {
      "type": "array",
      "description": "Allowed primitive type names. Every field type must resolve to one of these or to an entity/enum name.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "$ref": "#/definitions/primId",
            "description": "Unique identifier for this primitive type.",
            "x_idPattern": "prim"
          },
          "name": {
            "type": "string",
            "description": "Name of the primitive type (e.g., string, number, boolean, array, object)"
          },
          "description": {
            "type": "string",
            "description": "Description of the primitive type"
          }
        },
        "required": [
          "id",
          "name"
        ]
      },
      "default": [
        "string",
        "number",
        "boolean",
        "null",
        "any"
      ],
      "minItems": 1
    },
    "enums": {
      "type": "array",
      "description": "Enumerated types used across entities and API contracts.\nEnum names must be unique and follow PascalCase.\nEach enum must have at least one value following SCREAMING_SNAKE_CASE.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "values"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "$ref": "#/definitions/enumId",
            "description": "Unique identifier for this enum.",
            "x_idPattern": "num"
          },
          "name": {
            "type": "string",
            "description": "PascalCase enum name.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "description": {
            "type": "string",
            "description": "What this enum represents and its valid values."
          },
          "values": {
            "type": "array",
            "description": "All valid values for this enum.",
            "items": {
              "type": "object",
              "required": [
                "name"
              ],
              "additionalProperties": false,
              "properties": {
                "name": {
                  "type": "string",
                  "description": "SCREAMING_SNAKE_CASE constant name.",
                  "pattern": "^[A-Z][A-Z0-9_]*$"
                },
                "description": {
                  "type": "string",
                  "description": "What this enum value represents."
                }
              }
            },
            "minItems": 1
          }
        }
      }
    },
    "entities": {
      "type": "array",
      "description": "All domain entities (classes) in the system.\nEntity names must be unique and follow PascalCase.\nEach entity must have at least one field marked as primaryKey (or the first field is assumed).\nAbstract entities cannot be the target of composition/aggregation relationships.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "fields"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "$ref": "#/definitions/entId",
            "description": "Unique identifier for this entity.",
            "x_idPattern": "ent"
          },
          "name": {
            "type": "string",
            "description": "PascalCase entity name.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "description": {
            "type": "string",
            "description": "What this entity represents in the domain."
          },
          "abstract": {
            "type": "boolean",
            "description": "If true, this entity cannot be instantiated directly. Used for base classes.",
            "default": false
          },
          "extends": {
            "type": "string",
            "description": "Name of the parent entity this one inherits from. Must match an entity name in this spec.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "fields": {
            "type": "array",
            "description": "Data fields on this entity.\nField names must be unique and follow camelCase.\nField types must resolve to a primitive, entity name, or enum name defined in this spec.\nAt least one field should be marked as primaryKey.",
            "items": {
              "type": "object",
              "required": [
                "name",
                "type"
              ],
              "additionalProperties": false,
              "properties": {
                "name": {
                  "type": "string",
                  "description": "camelCase field name.",
                  "pattern": "^[a-z][A-Za-z0-9]*$"
                },
                "type": {
                  "type": "string",
                  "description": "Primitive name, entity name, enum name, or array thereof (e.g. 'string', 'User', 'OrderStatus', 'string[]', 'OrderItem[]')."
                },
                "required": {
                  "type": "boolean",
                  "description": "Whether this field must always be present.",
                  "default": true
                },
                "description": {
                  "type": "string",
                  "description": "What this field represents and how it is used."
                },
                "example": {
                  "description": "A concrete example value. Used in test spec and documentation.",
                  "type": [
                    "string",
                    "number",
                    "boolean",
                    "object",
                    "array",
                    "null"
                  ]
                },
                "primaryKey": {
                  "type": "boolean",
                  "description": "True if this field is the primary key for the entity. Only one field per entity should be marked as primaryKey. If not set, the first field or a field ending with 'Id'/'id' is assumed to be the primary key.",
                  "default": false
                },
                "glossaryRefs": {
                  "type": "array",
                  "description": "Glossary term IDs (GL-NNN) referenced in this field's description.",
                  "items": {
                    "$ref": "#/definitions/glId"
                  }
                }
              }
            }
          },
          "methods": {
            "type": "array",
            "description": "Methods exposed by this entity. Each must reference a function defined in the API spec.\nMethod names must follow camelCase.\napiRef must reference a function ID in the API spec (e.g. 'FN-createUser').",
            "items": {
              "type": "object",
              "required": [
                "name",
                "apiRef"
              ],
              "additionalProperties": false,
              "properties": {
                "name": {
                  "type": "string",
                  "description": "camelCase method name as it appears on the entity.",
                  "pattern": "^[a-z][A-Za-z0-9]*$"
                },
                "apiRef": {
                  "description": "The function ID in the API spec this method corresponds to. Format: FN-NNN-slug.",
                  "$ref": "#/definitions/fnId"
                },
                "description": {
                  "type": "string",
                  "description": "What this method does and how it relates to the API spec function."
                }
              }
            }
          },
          "visibility": {
            "type": "string",
            "description": "Whether this entity is part of the public API surface or internal only.",
            "enum": [
              "public",
              "internal"
            ],
            "default": "public"
          },
          "glossaryRefs": {
            "type": "array",
            "description": "Glossary term IDs (GL-NNN) that this entity maps to. The entity name itself should have a corresponding glossary entry.",
            "items": {
              "$ref": "#/definitions/glId"
            }
          },
          "componentRefs": {
            "type": "array",
            "description": "COMP-IDs from ArchitectureSpec components that own or serve this entity.",
            "items": {
              "$ref": "#/definitions/compId"
            }
          },
          "reqRefs": {
            "type": "array",
            "description": "REQ-IDs from GoalSpec that this entity supports. Used for entity\u2192requirement traceability.",
            "items": {
              "type": "string",
              "$ref": "#/definitions/reqId"
            }
          },
          "nfrRefs": {
            "type": "array",
            "description": "NFR-IDs from GoalSpec that this entity helps satisfy (e.g. performance, data quality).",
            "items": {
              "type": "string",
              "$ref": "#/definitions/nfrId"
            }
          },
          "constraintRefs": {
            "type": "array",
            "description": "CON-IDs from ArchitectureSpec constraints that affect this entity (e.g. data retention, partitioning).",
            "items": {
              "type": "string",
              "$ref": "#/definitions/conId"
            }
          }
        }
      }
    },
    "relationships": {
      "type": "array",
      "description": "All directional relationships between entities.\nRelationship types must be one of: association, composition, aggregation, dependency, realization.\nCardinality labels must be one of: 1, 0..1, 1..*, *, 0..*\nNo duplicate relationships of the same type between the same entity pair.\nAbstract entities cannot be the target of composition/aggregation relationships.",
      "items": {
        "type": "object",
        "required": [
          "from",
          "to",
          "type",
          "id"
        ],
        "additionalProperties": false,
        "properties": {
          "from": {
            "type": "string",
            "description": "Source entity name.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "to": {
            "type": "string",
            "description": "Target entity name.",
            "pattern": "^[A-Z][A-Za-z0-9]*$"
          },
          "type": {
            "type": "string",
            "description": "Relationship type. 'association': general link. 'composition': target cannot exist without source. 'aggregation': target can exist independently. 'dependency': source uses target but does not own it. 'realization': source implements target interface.",
            "enum": [
              "association",
              "composition",
              "aggregation",
              "dependency",
              "realization"
            ]
          },
          "cardinality": {
            "type": "object",
            "description": "Multiplicity on each end of the relationship.",
            "additionalProperties": false,
            "properties": {
              "fromLabel": {
                "type": "string",
                "description": "Multiplicity label on the source side, e.g. '1', '0..1', '1..*', '*'.",
                "enum": [
                  "1",
                  "0..1",
                  "1..*",
                  "*",
                  "0..*"
                ]
              },
              "toLabel": {
                "type": "string",
                "description": "Multiplicity label on the target side, e.g. '1', '0..1', '1..*', '*'.",
                "enum": [
                  "1",
                  "0..1",
                  "1..*",
                  "*",
                  "0..*"
                ]
              }
            }
          },
          "label": {
            "type": "string",
            "description": "Optional verb or role label on the relationship arrow, e.g. 'places', 'contains'."
          },
          "description": {
            "type": "string",
            "description": "What this relationship represents between the two entities."
          },
          "id": {
            "$ref": "#/definitions/relId",
            "description": "Unique identifier for this relationship.",
            "x_idPattern": "rel"
          },
          "reqRefs": {
            "type": "array",
            "description": "REQ-NNN IDs from GoalSpec functional requirements that require this relationship.",
            "items": {
              "$ref": "#/definitions/reqId"
            }
          }
        }
      }
    },
    "project": {
      "type": "string",
      "description": "Project name. Must match across all specs.",
      "minLength": 1
    },
    "goalSpecVersion": {
      "type": "string",
      "description": "Version of the GoalSpec this spec was reviewed against.",
      "pattern": "^v\\d+\\.\\d+\\.\\d+$"
    }
  },
  "outputFiles": [
    "artifacts/DataSpec.md",
    "artifacts/DataSpec.json"
  ],
  "specDependencies": {
    "goalSpec": {
      "required": true
    },
    "architectureSpec": {
      "required": true
    }
  }
}
